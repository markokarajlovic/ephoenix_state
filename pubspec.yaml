name: ephoenix_state
description: Lightweight annotations and code generator for equality and copyWith.
version: 0.1.0
homepage:

environment:
  sdk: ^3.0.0

dependencies:
  analyzer: '>=9.0.0 <11.0.0'
  build: ^4.0.5
  source_gen: ^4.2.2

dev_dependencies:
  build_runner: ^2.13.1
  lints: ^6.1.0
  test: ^1.31.0
